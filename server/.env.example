# Copy to .env and edit
# Optional dev-only settings
ADMIN_USER=admin
ADMIN_PASS=password
# Set a strong secret for JWT in production
JWT_SECRET=change_this_to_a_strong_secret
WORKER_TOKEN=workerdevtoken
PORT=3000
